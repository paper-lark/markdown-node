{
  "name": "markdown-node",
  "version": "0.1.0",
  "description": "Lightweight Markdown parser module for Node.js",
  "main": "/lib/parser.js",
  "scripts": {
    "compile": "tsc",
    "watch": "tsc --watch",
    "prestart": "yarn compile",
    "start": "node ./index.js",
    "pretest": "yarn compile",
    "test": "jest test/*.js"
  },
  "author": "Max Zhurasky <paperlark@yandex.com>",
  "license": "MIT",
  "dependencies": {
    "prismjs": "^1.13.0"
  },
  "devDependencies": {
    "@types/node": "^9.4.7",
    "jest": "^22.4.3"
  }
}
